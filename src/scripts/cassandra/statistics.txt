CREATE KEYSPACE "statistics" WITH REPLICATION = { 'class' : 'SimpleStrategy' , 'replication_factor' :2 };
CREATE KEYSPACE "statistics" WITH REPLICATION = { 'class' : 'SimpleStrategy' , 'replication_factor' :2 };
CREATE TABLE cpu_avg_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));
CREATE TABLE cpu_top_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));
CREATE TABLE cpu_bottom_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));
CREATE TABLE ram_avg_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));
CREATE TABLE ram_top_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));
CREATE TABLE ram_bottom_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));
CREATE TABLE upTime_top_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));
CREATE TABLE activeSessions_top_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));
CREATE TABLE activeSessions_bottom_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));
CREATE TABLE activeSessions_sum_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));
CREATE TABLE activeSessions_avg_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));
CREATE TABLE temperature_avg_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));
CREATE TABLE temperature_top_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));
CREATE TABLE temperature_bottom_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));


drop table activesessions_top_statistics;drop table activesessions_sum_statistics;drop table cpu_bottom_statistics;drop table temperature_top_statistics;drop table cpu_avg_statistics;drop table activesessions_bottom_statistics;drop table ram_avg_statistics;drop table uptime_top_statistics;drop table cpu_top_statistics;drop table temperature_bottom_statistics;drop table activesessions_avg_statistics;drop table ram_bottom_statistics;drop table ram_top_statistics;drop table temperature_avg_statistics;CREATE TABLE cpu_avg_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));CREATE TABLE cpu_top_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));CREATE TABLE cpu_bottom_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));CREATE TABLE ram_top_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));CREATE TABLE ram_bottom_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));CREATE TABLE upTime_top_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));CREATE TABLE activeSessions_top_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));CREATE TABLE activeSessions_bottom_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));CREATE TABLE activeSessions_sum_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));CREATE TABLE activeSessions_avg_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));CREATE TABLE temperature_avg_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));CREATE TABLE temperature_top_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));CREATE TABLE temperature_bottom_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));CREATE TABLE ram_avg_statistics (id int,name  text,site text,eventTime timestamp,  value float, duration int, PRIMARY KEY (id,eventTime));
